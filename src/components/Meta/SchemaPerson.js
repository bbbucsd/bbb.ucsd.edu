import React, { Component } from 'react';
import PropTypes from 'prop-types';

class SchemaPerson extends Component {

  render() {

    return (
      <script>
        {/* JSON.stringify() */}
      </script>
    );
  }
}

SchemaPerson.propTypes = {
  schema: PropTypes.object.isRequired,
}
